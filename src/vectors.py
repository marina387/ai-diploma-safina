# Создание векторов (эмбеддингов)

import numpy as np
# В дипломе: превращаем текст резюме в вектор
vector = np.array([0.1, -0.3, 0.5, ...])  # 384 числа


# Длина вектора (норма)
len_v = np.linalg.norm(vector)
#Нужна для нормализации векторов и расчета косинусной близости.

# Скалярное произведение
similarity = np.dot(vec_resume, vec_vacancy)
#Показывает близость кандидата и вакансии.

#Косинусная близость
cosine = np.dot(v1, v2) / (np.linalg.norm(v1) * np.linalg.norm(v2))